@model IEnumerable<Model1.DTO.PhieuxuatDTO>
@{
    ViewBag.Title = "Index11";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

@section header{
    Phiếu cấp phát
}

<div class="row">

    <div class="col-lg-pull-3" style="padding:10px">

        <div class="section group">
            <b class="panel-heading" style="color:black; font-size:large">
                Danh sách phiếu cấp phát
                @*<a class="btn btn-primary" href="/Admin/Capphat/Create" style=" margin:0 0 0 36px">Thêm</a>*@
            </b>
            <hr />

            <div class="table-responsive">
                <table class="table table-striped table-bordered table-hover">
                    <thead>
                        <tr>
                            <th>Tên điểm tiêm</th>
                            <th>Ngày xuất</th>
                            <th>Tình trạng</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model != null)
                        {
                            foreach (var item in Model)
                            {
                                <tr>
                                    <td>@item.Tendiemtiem</td>
                                    <td>@item.Ngayxuat</td>
                                    <td>@item.Tentt</td>
                                    <td>
                                        <a href="/Admin/Capphat/Edithd/@item.Sophieuxuat">Sửa</a>
                                        |
                                        @Ajax.ActionLink("Xoá", "Delete", new { @id = item.Sophieuxuat }, new AjaxOptions
                                   {
                                       Confirm = "Bạn có muốn xoá bản ghi này?",
                                       OnComplete = "$('#row_" + @item.Sophieuxuat + "').remove()",
                                       HttpMethod = "Delete"
                                   })|
                                        <a href="/Admin/Capphat/Detailhd/@item.Sophieuxuat">Chi tiết</a>
                                    </td>
                                </tr>
                            }
                        }

                    </tbody>
                </table>

            </div>
        </div>
        <link href="~/Content/themes/base/jquery-ui.min.css" rel="stylesheet" />
